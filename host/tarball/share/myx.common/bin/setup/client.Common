#!/bin/sh

case "$0" in
	*/myx.common/bin/setup/client.Common) 
		if [ "$1" = "--help" ] ; then
			. "${MYXROOT:-/usr/local/share/myx.common}/help/setup/client.help.include" >&2
			exit 1
		fi
		set -e
		myx.common setup/console
		myx.common setup/completion
	;;
esac

