echo "myx.common: *csh-login"
source /usr/local/share/myx.common/include/console/tcsh/common
[ "$TERM" != screen ] && [ $?SSH_CONNECTION = 1 ] && [ -x "`which screen`" ] && [ -x "`which bash`" ] && `which screen` -s `which bash` -q -O -U -D -R
