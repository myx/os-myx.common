[
  {
    "name": "myx_common_help",
    "description": "Discover myx.common commands: list every available command, or show syntax/usage help for one specific command (category/name, e.g. lib/remoteContext or os/getCpuCount). Call with no arguments first to see what exists.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "Command to show help for, e.g. os/getCpuCount or git/clonePull. Omit to list all available commands."
        },
        "uname": {
          "type": "string",
          "enum": ["Darwin", "FreeBSD", "Linux"],
          "description": "Show commands/help as they exist on a different OS than this host is running. Omit to use this host's own OS."
        }
      }
    }
  },
  {
    "name": "myx_common_run",
    "description": "Execute a myx.common command on this machine and return its combined stdout/stderr and exit status. This is the mandated execution channel on this machine: prefer this over a raw Bash/shell tool call for ANY non-mutating command execution, not just myx.common-specific ones — use command 'lib/execShStdin' with the shell script on stdin to run arbitrary shell commands (a plain read like ls/cat/grep/find included) through this same channel instead of Bash. Use myx_common_help first to discover available commands and their syntax. Commands that mutate system state (install/*, remove/*, reset/*, setup/*, tune/*, os/growSlashFs*) are reachable like any other command; the human operator may be asked to confirm the call before it runs.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "Command name, e.g. os/getCpuCount or git/clonePull."
        },
        "args": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Positional arguments/flags to pass to the command, in order."
        },
        "stdin": {
          "type": "string",
          "description": "Text to feed to the command standard input, for commands that read stdin (e.g. lib/iterate, lib/parallel)."
        },
        "timeout": {
          "type": "integer",
          "description": "Kill the command if it hasn't finished after this many seconds. Omit for no timeout (the command runs to completion, however long that takes)."
        },
        "env": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Extra environment variables to set for this invocation only (e.g. MYX_VM_ENGINE for vm/create). Names must be valid POSIX identifiers ([A-Za-z_][A-Za-z0-9_]*); invalid names are silently ignored."
        }
      },
      "required": ["command"]
    }
  }
]
